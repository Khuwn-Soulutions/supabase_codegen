name: supabase_codegen
description: Supabase Codegen
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  args: ^2.6.0
  collection: ^1.19.1
  dotenv: ^4.2.0
  latlng: ^2.0.7
  meta: ^1.16.0
  path: ^1.9.1
  quiver: ^3.2.2
  supabase: ^2.6.3

dev_dependencies:
  test: ^1.25.15
  very_good_analysis: ^7.0.0

executables:
  generate_types:
  add_codegen_functions:
